#include "router.h"

Router::Router()
{

}
/*
void Router::add_route(int number_route, Data_route data_route)
{
    database_routes[number_route] = std::move(data_route);
}

const Data_route& Router::get_route(int number_route) const
{
    const auto it = database_routes.find(number_route);
    // гарантируется, что маршрут есть
    return it->second;
}
*/
